doctype html
html
  head
    meta(http-equiv='Content-Type', content='text/html; charset=utf-8')
    meta(name='viewport', content='width=device-width,minimum-scale=1.0, maximum-scale=1.0')
    title Site Name
    style.
      @media screen and (max-device-width: 480px) {
        body {
          -webkit-text-size-adjust: none
        }
      }
    script(src='/js/browser-deeplink.js', type='text/javascript')
    script.
      deeplink.setup({
        iOS: {
          appName: "mybhn",
          appId: "942608209",
        },
        android: {
          appId: "com.spectrum.myspectrum"
        }
      });
      var open = function (uri) {
        var iframe = document.createElement("iframe");
        iframe.onload = function () {
          alert('iframe.onload triggered');
          window.location.href = uri;
        };
        iframe.src = uri;
        document.body.appendChild(iframe);

        return true;
      }
      window.onload = function () {
        open("www.google.com");
      //   <!-- Deep link URL for existing users with app already installed on their device -->
      //   window.location = 'youtube://';
      //   <!-- Download URL (TUNE link) for new users to download the app -->
      //   setTimeout("window.location = 'https://spectrum.net';", 1000);
      }
  body
    block content
